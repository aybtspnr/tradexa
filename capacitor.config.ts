import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.tradexa3loginv3',
  appName: 'Tradexa 3 Login v3',
  webDir: 'dist'
};

export default config;
